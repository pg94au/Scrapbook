This project resides in a GitHub repository, and it should be built every time a commit or merge to the main branch occurs.
There should be a github file written to perform this action.  It should build the solution and run the tests, with the build
failing if either step fails.

Any files related to GitHub workflows should be included in a solution folder in the existing solution so that they are
visible to a user who opens the solution in Visual Studio.
