# Copilot Instructions

## Project Guidelines
- User prefers command scripts to use newline as the statement separator to avoid cross-line parsing ambiguity.
- Always ensure markdown use cases are reproduced precisely in tests: scripts exactly verbatim, setup aligned to spec intent, and assertions aligned to spec intent; if exact compliance is not possible, explicitly inform the user immediately.